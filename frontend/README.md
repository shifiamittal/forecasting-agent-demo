# Static frontend

See the root README for demo scope, architecture and verification notes.

Run `npm ci`, then `npm run dev`. Build with `npm run build`; publish only `dist`.
No backend, API keys or environment variables are required. All data is fictional and bundled locally.
